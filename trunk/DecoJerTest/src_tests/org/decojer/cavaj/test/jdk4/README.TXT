Differences to previous

* 1.4 introduced assert.


* new startup param "source" (previously just "-target=1.3")
* "-source=1.4" for new language feature assert necessary

* conditional compound (ternary operator) now generates a none-flat forward-edge CFG
