Differences JDK 2 to previous

* heavy Bugs with Inner Classes in JDK 1, e.g. not useable: Inner2.Inner11.class
    Can't access inner class org.decojer.cavaj.test.DecTestInner. Inner1.Inner11. Only public classes and interfaces in other packages can be accessed.
    import org.decojer.cavaj.test.DecTestInner.Inner1.Inner11;